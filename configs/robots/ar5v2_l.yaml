# 单独机械臂示例配置。
robot:
  # Isaac 场景中的机器人名称。
  name: ar5v2_l
  # 当前默认使用 MJCF 资产导入；也可扩展为 urdf。
  asset_type: mjcf
  # 机器人资产路径，相对仓库根目录。
  asset_path: assets/single_system/arm/AR5V2_L/AR5V2_L.xml
  # 导入到 USD stage 的根 prim 路径。
  prim_path: /World/AR5V2_L
  import:
    # 导入碰撞 mesh 时使用的近似方式：convex_decomposition 或 convex_hull。
    collision_approximation: convex_decomposition
  physics:
    gravity:
      default: false
      arm: false
      hand: false
    physx:
      material:
        contact_static_friction: 0.8
        contact_dynamic_friction: 0.6
        contact_restitution: 0.0
      rigid_body:
        linear_damping: 0.0
        angular_damping: 0.1
    solver:
      arm:
        position_iterations: 32
        velocity_iterations: 4

cumotion:
  # 机器人模型资源字段；IK/planner 等算法参数由 configs/cumotion/*.yaml 提供。
  xrdf_path: assets/single_system/arm/AR5V2_L/AR5V2_L.xrdf
  urdf_path: assets/single_system/arm/AR5V2_L/AR5V2_L.urdf
  flange_frame: AR5V2_L_arm_flan_link
