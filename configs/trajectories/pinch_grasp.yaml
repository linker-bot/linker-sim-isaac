# 机械臂+灵巧手捏合抓取绳体端块的脚本任务配置。
grasp:
  # 抓取左端或右端 endpoint cuboid。
  endpoint: left
  # 在端块中心基础上添加的 TCP 目标偏移，单位 m。
  target_world_offset: [0.02, 0.0, 0.03]
  # 目标 TCP 姿态，固定轴 XYZ 顺序（外旋 XYZ 顺序）的 RPY，单位 rad；任务内部会转成 wxyz 四元数。
  target_rpy: [0.0, 2.007128639793479, -1.5707963267948966]
  # false 时只约束 TCP 位置，忽略姿态。
  use_orientation: true
  # approach 点在 grasp 点上方的距离。
  approach_distance: 0.10
  # 抓住后向上 lift 的高度。
  lift_height: 0.4
  # 各阶段持续时间，单位 s。
  prep_duration: 1.0
  move_duration: 3.0
  approach_duration: 1.2
  close_duration: 1.0
  lift_duration: 2.0
  # lift 后沿 wiggle_axis 左右摆动，用于观察绳体交互和抓取稳定性。
  wiggle_cycles: 2
  wiggle_amplitude: 0.2
  wiggle_duration: 2.0
  wiggle_axis: [1.0, 0.0, 0.0]
  # 最终保持时间；--hold 时脚本可继续保持到窗口关闭。
  final_hold_duration: 5.0
  # 抓取后额外扫动第 1 轴，帮助观察动态响应。
  post_joint_sweep_duration: 2.0
  post_joint_sweep_targets: [2.1, -2.1]
  # 从 approach 点下沉到抓取点时，TCP 直线 IK waypoint 的采样频率。
  approach_line_sample_hz: 100.0
  # 写入临时 cuMotion URDF 的 TCP frame 名。
  tcp_frame_name: pinch_tcp
  # 关节角到关节角阶段的 cuMotion 路径/轨迹规划参数。
  motion_planning:
    # true 时优先使用 cuMotion 返回的 interpolated_path；false 时只使用 planner 的 sparse path。
    path_interpolation: False
    # time_optimal 让 cuMotion 按机器人约束自动时间参数化；time_stamped 使用阶段时长生成时间戳。
    trajectory_mode: time_optimal # time_stamped # time_optimal
    # trajectory_mode=time_stamped 时生效，可选 linear 或 cubic_spline。
    interpolation_mode: cubic_spline
  # 接近目标前的半开手型。
  pre_pinch_hand_targets:
    L6V1_L_hand_thumb_cmc_roll: 0.95
    L6V1_L_hand_thumb_cmc_pitch: 0.28
    L6V1_L_hand_index_mcp_pitch: 0.25
    L6V1_L_hand_middle_mcp_pitch: 0.15
    L6V1_L_hand_ring_mcp_pitch: 0.15
    L6V1_L_hand_pinky_mcp_pitch: 0.12
  # 闭合抓取手型；pinch TCP 也会基于这个闭合几何计算。
  closed_pinch_hand_targets:
    L6V1_L_hand_thumb_cmc_roll: 0.95
    L6V1_L_hand_thumb_cmc_pitch: 0.7
    L6V1_L_hand_index_mcp_pitch: 0.85
    L6V1_L_hand_middle_mcp_pitch: 0.45
    L6V1_L_hand_ring_mcp_pitch: 0.4
    L6V1_L_hand_pinky_mcp_pitch: 0.35
