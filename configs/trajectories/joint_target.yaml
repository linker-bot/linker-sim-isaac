# 固定时间关节目标轨迹。
trajectory:
  type: joint_target
  # 从当前关节位置过渡到 targets 的总时长，单位 s。
  duration: 2.0
  # 轨迹采样频率；控制器会逐点下发。
  sample_hz: 200.0
  # 时间缩放函数：smoothstep 端点速度为 0，适合 smoke/demo。
  interpolation: smoothstep
  # 稀疏目标：未列出的关节保持当前值。
  targets:
    AR5V2_L_arm_joint_1: 0.4
    AR5V2_L_arm_joint_2: 0.2
    AR5V2_L_arm_joint_3: 0.3
    AR5V2_L_arm_joint_4: 0.2
    AR5V2_L_arm_joint_5: 0.9
    AR5V2_L_arm_joint_6: 0.4
    AR5V2_L_arm_joint_7: 0.2
