"""关节轨迹数据结构与构造工具。

该包只负责保存和构造项目统一的 ``JointTrajectory``，不直接调用 Isaac，也不做 IK 或运动
规划。调用方可以把输出的关节轨迹交给 execution 层按仿真时钟播放。

轨迹矩阵行对应采样时刻，列对应调用方传入的关节顺序；本包不会重新排序 DOF。时间
单位为秒，角度单位为弧度，速度/加速度/jerk 由差分或后端输出保持一致。
"""
